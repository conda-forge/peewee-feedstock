schema_version: 1

context:
  name: peewee
  version: "4.1.1"

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 6fb1782252046e088599ed005342d7db6d17d48cacea27bc3c9a74f04646224d

build:
  number: 0
  script:
    env:
      NO_SQLITE: ${{ "1" if win else "0" }}
    content:
      - python -m pip install . -vv --no-build-isolation --no-deps

  python:
    entry_points:
      - pwiz = pwiz:main

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - cython
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
  host:
    - python
    - pip
    - cython >=0.22.1
    - if: win
      then:
        - msinttypes
    - sqlite
    - setuptools
    - wheel
  run:
    - python

tests:
  - python:
      imports:
        - playhouse
        - peewee
      pip_check: true
  - script:
      - pwiz --help

about:
  license: MIT
  license_file: LICENSE
  summary: A little ORM
  homepage: https://github.com/coleifer/peewee/
  repository: https://github.com/coleifer/peewee/
  documentation: http://docs.peewee-orm.com/en/latest/

extra:
  recipe-maintainers:
    - dharhas
    - synapticarbors
